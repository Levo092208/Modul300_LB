Vagrant.configure("2") do |config|
  # Box Settings
  config.vm.box = "ubuntu/xenial64"
  # Provider Settings
  config.vm.provider "virtualbox" do |vb|
  vb.name = Vagrant-VM-1
  vb.gui = true
  vb.memory = "4096"
  config.vm.network "public_network"
 end